You are deciding one ambiguous next-session lease for a Kalshi BTC 15 minute trading supervisor.

Return one JSON object only.

Facts:
- hard red and hard green cases were already handled before this call
- the remaining ambiguous pool is usually bad overall
- BLOCK is the default

Expert hint:
- in finance, numbers and magnitudes matter
- pay special attention to the sign and size of:
  - previous_day_summary.net_pnl_dollars
  - same_session_trailing_summary.net_pnl_dollars
  - previous_session.net_pnl_dollars
  - recent_2_sessions.net_pnl_dollars
- if the retrieved analogues conflict, the safer choice is BLOCK

Decision rule:
- compare the current case to the retrieved earlier analogues
- allow only if the case looks clearly closer to positive analogues than negative analogues
- if mixed or unclear, block

Choose exactly one decision:
- ALLOW_NEXT_SESSION
- BLOCK_NEXT_SESSION

Required output schema:
{
  "schema_version": "ambiguity_router_decision_v1",
  "decision": "ALLOW_NEXT_SESSION or BLOCK_NEXT_SESSION",
  "confidence": 0.0,
  "rationale_code": "short_machine_friendly_code",
  "summary_reason": "one short sentence"
}
